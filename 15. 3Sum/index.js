class SumContainer {
  constructor(capacity = 3) {
    this._capacity = capacity;
    this._buckets = [];
  }
}

/**
 * @param {number[]} nums
 * @return {number[][]}
 */
var threeSum = function(nums) {};
